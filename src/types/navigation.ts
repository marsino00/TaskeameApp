export type RootStackParamList = {
    TabNavigator: undefined; // si no hay parámetros
    Login: undefined; // si no hay parámetros
    // Si tu TabNavigator acepta parámetros, puedes definirlos aquí, por ejemplo:
    // TabNavigator: { userId: string };
  };
